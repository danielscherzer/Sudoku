<UserControl x:Class="WpfSudoku.View.Board"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="clr-namespace:WpfSudoku.ViewModel"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="450" d:DataContext="{DynamicResource grid}" 
			 Width="{Binding RelativeSource={RelativeSource Self}, Path=ActualHeight}"
			 HorizontalAlignment ="Stretch" HorizontalContentAlignment ="Stretch" VerticalAlignment ="Stretch" VerticalContentAlignment ="Stretch">
	<UserControl.Resources>
		<vm:BoardViewModel x:Key="grid"/>
		<Style x:Key="cellBorder" TargetType="Border">
			<Setter Property="BorderBrush" Value="{DynamicResource borderBrush}"/>
			<Setter Property="Background" Value="{DynamicResource backgroundBrush}"/>
		</Style>
		<DataTemplate x:Key ="CellTemplate">
			<Border Style="{StaticResource cellBorder}" Width="30" Height="30" BorderThickness="0.1">
				<TextBlock HorizontalAlignment ="Center" VerticalAlignment ="Center" FontSize="24" Foreground="{DynamicResource borderBrush}" Text ="{Binding Path=Value}"/>
			</Border>
		</DataTemplate>
		<DataTemplate x:Key ="BlockTemplate">
			<Border Style="{StaticResource cellBorder}" BorderThickness="1" Margin="-0.5">
				<ItemsControl ItemsSource ="{Binding Path=Items}" ItemTemplate ="{StaticResource CellTemplate}">
					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<UniformGrid Background="{DynamicResource borderBrush}"/>
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>
				</ItemsControl>
			</Border>
		</DataTemplate>
	</UserControl.Resources>
	<Viewbox>
		<ItemsControl ItemsSource ="{Binding Path=Blocks}" ItemTemplate ="{StaticResource BlockTemplate}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<UniformGrid Background="{DynamicResource borderBrush}"/>
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
		</ItemsControl>
	</Viewbox>
</UserControl>
